function SectionDivider() {
  return (
    <div
      className="w-full h-px my-12"
      style={{
        background:
          'linear-gradient(90deg, transparent, var(--color-accent-pink), var(--color-accent-purple), var(--color-accent-blue), transparent)',
      }}
    />
  );
}

export default SectionDivider;
